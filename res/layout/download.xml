<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    >
    
    <Button android:id="@+id/launch_music_app"
    		android:layout_width="fill_parent"
    		android:layout_height="wrap_content"
    		android:layout_alignParentBottom="true"
    		android:text="Launch Music Player"
    		/>
	<TextView 
		android:id="@+id/dl_header"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:background="#467aad"
		android:text="Pending Downloads"
		android:textAppearance="?android:attr/textAppearanceLarge"
		android:paddingLeft="10dip"
		android:paddingTop="10dip"
		android:paddingBottom="10dip"
		/>
	<ListView
		android:id="@id/android:list" 
		android:layout_width="fill_parent" 
		android:layout_height="fill_parent"
		android:layout_below="@id/dl_header"
		android:layout_above="@id/launch_music_app"
	/>
</RelativeLayout>
